---
name: timeline
description: Choreograph explicit, readable GSAP timing and truthful Motionly track metadata.
---

# Timeline

Use explicit positions in seconds and truthful scene-local track spans. Overlap related actions so the film does not feel queued, but do not overlap competing messages. A global retime changes nested timeline scale and metadata together; fps changes samples, not speed.

Most entrances should settle in roughly 0.45â€“0.8 seconds. Allow enough hold time to read completed text and inspect product UI. Initialize state at time zero and cleanly resolve every outgoing layer before its ownership ends.
